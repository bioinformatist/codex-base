interface:
  display_name: "Resolving Merge Conflicts"
  short_description: "Resolve active Git conflicts without finishing Git state"
  default_prompt: "Use $resolving-merge-conflicts to resolve the current merge or rebase conflicts safely."
policy:
  allow_implicit_invocation: true
